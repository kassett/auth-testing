services:

  coredns:
    image: coredns/coredns:latest
    command: -conf /Corefile
    volumes:
      - ./Corefile:/Corefile:ro
      - ./db.autheval.test:/zones/db.autheval.test:ro
    ports:
      - "53:53/tcp"
      - "53:53/udp"