services:

  coredns:
    image: coredns/coredns:latest
    command: -conf /Corefile
    volumes:
      - ./Corefile:/Corefile:ro
      - ./db.kassett.test:/zones/db.kassett.test:ro
    ports:
      - "53:53/tcp"
      - "53:53/udp"